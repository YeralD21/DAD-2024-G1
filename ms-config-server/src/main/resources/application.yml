server:
  port: 7071

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/YeralD21/DAD-2024-G1 # ruta del repositorio git
          searchPaths: config-data
          default-label: main
          #username:
          #password:


